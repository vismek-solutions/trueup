---
title: Loose
---

# A heading

This sentence uses an em dash — right here.

Leverage the thing.

This one sentence is deliberately written to run well past the twelve word limit set here.

One. Two. This third sentence is deliberately written to run past the twelve word limit here.

Use the `run the whole thing now` phrase in code.

The report names the file. The report names the file it names.
