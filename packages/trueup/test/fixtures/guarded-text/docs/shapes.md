---
title: Shapes — here
---

This first part is short. **Commit it.** Nothing else.

Read trueup.config.ts. Then run it.

Run `packages/trueup/src/text/passages.ts` now.

Run `pnpm run check --prefix packages/trueup` now.

An option written out in prose, such as "--dots", names a flag rather than a pause.

<!-- a note holding — a banned mark
     and leverage, a banned word, on the line that closes it -->

One line of prose after it. Nothing else here.
