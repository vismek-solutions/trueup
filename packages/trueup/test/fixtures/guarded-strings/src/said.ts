export const said = "leverage the cached total — the caller summed it";

export const whole = `The report names the file. It names the claim. It names the line.

The remedy follows it.`;

export const built = [
  "The report names the file.",
  "",
  "It names the claim as well.",
].join("\n");

export const where = "src/**/*.ts";

export const counted = (value: number): string => `leverage the cached total of ${value} lines`;
