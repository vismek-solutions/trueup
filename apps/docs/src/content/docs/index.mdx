---
title: trueup
description: Keeps a TypeScript codebase in the shape you meant it to have, and stops a coding agent from quietly changing it.
template: splash
hero:
  tagline: Keeps a TypeScript codebase in the shape you meant it to have — and stops a coding agent from quietly changing it.
  actions:
    - text: Getting started
      link: /start/getting-started/
      icon: right-arrow
    - text: What it checks
      link: /checks/
      icon: list-format
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

`trueup` reads your source files without running them, works out which file depends on which, and checks that map against rules you wrote down. When something breaks a rule, it names the file, says what it did, and says what to do about it.

## It reads between files, not inside them

A linter reads one file at a time and tells you about that file. An unused variable. A missing `await`. It is looking at the code *inside* a file.

`trueup` looks at the lines *between* files. Which parts of your project may know about which other parts. Where things live. Whether a folder has quietly become a junk drawer.

None of that is visible from inside any single file, which is why the shape drifts without anyone noticing.

You describe the shape once, in a config file. Every run after that answers one question: is this still true?

## Why it exists

Every edit an agent makes is defensible on its own. Reaching into another module is the shortest path to a working feature, the tests stay green, and after a hundred of them the project is a shape nobody chose. You cannot review your way out of it — not reading every line is the point.

So the constraints have to live where the agent runs into them, every time, without you. And because an agent told to get the build green will widen a rule as readily as fix the code, every message this prints says which one is meant.

<CardGrid>
  <Card title="Sees through re-exports" icon="puzzle">
    Most projects have an `index.ts` that re-exports a folder. Other tools then see everyone importing `shared/index.ts` and can tell nobody apart. This one follows the chain to the file that declares what you imported.
  </Card>
  <Card title="Refuses a bad edit" icon="error">
    Run it as a Claude Code hook and it judges a proposed write before it lands, with the reason the agent needs to correct course in the same turn.
  </Card>
  <Card title="Fails loudly" icon="warning">
    An import that does not resolve, a zone matching nothing, a file no rule covers: each is an error. A check that passes while enforcing nothing is worse than no check.
  </Card>
  <Card title="One report" icon="document">
    Your linter's findings show up in the same report, with the same pass or fail.
  </Card>
</CardGrid>

New to it? [Getting started](/start/getting-started/) sets it up on a normal project in about ten minutes.
