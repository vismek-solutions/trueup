{
  "name": "trueline-repo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "check": "node ./packages/trueline/bin/trueline.js",
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "pnpm --filter trueline run test",
    "typecheck": "pnpm --filter trueline run typecheck",
    "build": "pnpm --filter trueline run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "fallow": "^3.22.0",
    "oxlint": "^1.81.0"
  }
}
